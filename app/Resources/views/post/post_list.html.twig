<div class="row list-group">
    {% for post in posts %}
        <article class="col-xs-12 list-group-item">
            <h4>
                <a class="read-more" href="{{ path('post_show', {'id': post.id }) }}">{{ post.name }}</a>
            </h4>
            <span class="category">Категория:&nbsp;{{ post.category }}</span>
        </article>
    {% endfor %}
</div>