{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Post creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Создать" />
    {{ form_end(form) }}

    <div class="controls">
        <ul>
            <li>
                <a href="{{ path('helkiper_blog_index') }}">На главную</a>
            </li>
        </ul>
    </div>
{% endblock %}
